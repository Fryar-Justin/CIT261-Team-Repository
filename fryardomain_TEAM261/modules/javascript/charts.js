var created = false;

function main(){
    var instrument = document.getElementById("instrument").value;      // get the instrument selected
    // display the instrument selected
    var element = document.getElementById("fingeringCharts");
    var h1 = document.createElement("h1");
    var node = document.createTextNode(instrument);
    
    // create a canvas element
    
    // display the image
}